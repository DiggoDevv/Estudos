const numeros = [100, 200, 300, 400, 500, 600];
       // o que está dentro das "{}" é oq vai ser executado várias vezes
// for (let indice = 0; indice < 6 ;      indice++ ) {
    // inicio             // condição   //somar 1 ao valor repetidamente
    // Primeira expressão: é executada apenas uma única vez.
    // Segunda expressão: condição de execução
    // Terceira expressão: é executada sempre ao final do bloco
   // console.log("oi");
   //repete "oi" 6 vezes (enquanto "oi" for menor que 6, adicione +1)
//    for (let indice = 0; indice < numeros.length ;      indice++ ) {
//    console.log(numeros[indice]);
// }
